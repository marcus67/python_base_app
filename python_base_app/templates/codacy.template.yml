engines:
  duplication:
    enabled: true
    exclude_paths:
      - {{ var.setup.module_name }}/test/**
      - alembic/versions/**

exclude_paths:
  - '{{ var.setup.module_name }}/templates/**'
  - '{{ var.setup.module_name }}/static/contrib/**'
